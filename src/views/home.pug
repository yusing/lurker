include ../mixins/sub
doctype html
html
  +subMgmt()
  script(async).
    window.addEventListener("load", () => {
      const url = getSubs().join('+');
        if (url) {
          window.location.href = '/r/${url}';
        } else {
          window.location.href = '/r/all';
        }
    });
